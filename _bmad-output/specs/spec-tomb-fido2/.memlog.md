---
topic: tomb-fido2: FIDO2-exclusive LUKS2 unlock CLI (Go/Rust), replacing GPG/keyfile-based cryptsetup+fido2-token+systemd-cryptenroll workflow
updated: 2026-08-08T01:52
---

- (note) source: _bmad-output/brainstorming/brainstorm-tomb-fido2-2026-07-21/brainstorm-intent.md and its .memlog.md, fully absorbed
- (decision) Why: vision to realize + pain to solve. Cold-storage crisis reliability (bank-safe-once-a-year model) for rarely-accessed sensitive material; replaces the multi-tool memorization burden of cryptsetup+fido2-token+systemd-cryptenroll; TrueCrypt's 2014 shutdown is the cautionary reference for standard-tools-only design
- (capability) CAP-1 intent: user can unlock a LUKS2 volume using a FIDO2 security key via tomb-fido2. success: a user with zero FIDO2 familiarity unlocks a LUKS2 volume on first try after being physically prompted to touch the key, no external docs consulted
- (capability) CAP-2 intent: user can enroll an additional FIDO2 key as an alternate unlock method on an already-unlocked volume (LUKS2 native multi-keyslot, up to 32 slots). success: after enrollment, the volume unlocks with either the original or the newly enrolled key
- (capability) CAP-3 intent: user can revoke a single FIDO2 key's keyslot, removing it as a valid unlock method. success: after revocation, the removed key no longer unlocks the volume while other enrolled keys still do
- (capability) CAP-4 intent: user drives unlock/enroll/revoke/pre-flight-check through one unified CLI instead of separately invoking cryptsetup, fido2-token, and systemd-cryptenroll. success: no workflow in v1 scope requires the user to invoke the three underlying tools directly
- (capability) CAP-5 intent: tool guides the user through every interactive step (e.g. key touch) in plain language assuming zero FIDO2 knowledge. success: prompts and error messages never assume familiarity with FIDO2 terms (e.g. 'Please touch your security key button', not 'Awaiting UP')
- (capability) CAP-6 intent: tool verifies all hard dependencies (LUKS2 FIDO2/hmac-secret support, required binaries, kernel features) before any operation. success: on missing dependency, tool exits cleanly pre-operation with a detailed, actionable error message rather than failing mid-operation
- (capability) CAP-7 intent: user can apply all tomb-fido2 operations (unlock/enroll/revoke) directly to raw physical block devices/partitions, not only file-backed loop devices. success: the same CLI commands work unmodified against a raw partition path and a loop-mounted file, a capability original Tomb never exposed
- (constraint) Standard, low-level, well-tested primitives only: LUKS/dm-crypt + FIDO2 hmac-secret. No proprietary formats, no single-vendor-maintained crypto - rules out any custom key-wrapping or format
- (constraint) Break-glass recoverability: README must document manually unlocking the volume using only bare cryptsetup + fido2-token commands, with zero dependency on the tomb-fido2 binary surviving - rules out any proprietary on-disk metadata the binary alone can interpret
- (constraint) Zero-FIDO2-knowledge UX is mandatory across all prompts and errors, not just the happy path - rules out terse/technical CLI output as a valid default
- (constraint) Physical key presence at the exact moment of unlock is a fixed, non-variable assumption - tool supports exactly what cryptsetup's FIDO2 token mode natively offers, no more
- (constraint) No fallback auth paths: FIDO2 is the exclusive unlock mechanism, no GPG/keyfile fallback - rules out any 'recovery password' or keyfile escape hatch as a built-in feature
- (constraint) Must be a real compiled binary (Go or Rust), not a shell script wrapper
- (constraint) Tool remains entirely unaware of backup strategy: README may recommend 3-2-1-style backup of volumes/keys as a disclaimer only, but no code or feature may touch backup concerns
- (non-goal) Remote/delegated unlock beyond cryptsetup's native FIDO2 token mode is out of scope this time
- (non-goal) Post-quantum-readiness features are out of scope - standard-tools recoverability is considered future-proof enough
- (assumption) Should-Have items (break-glass README procedure, 3-2-1 backup disclaimer) are treated as Constraints in the spec kernel since they bend design/documentation decisions rather than describing tool behavior a capability would test
- (question) Recognized safety gap: input does not address what happens if a user attempts to revoke the last remaining valid keyslot (risk of permanent lockout), despite survivability being the project's central design goal - needs a human decision before CAP-3 is implementation-ready
- (question) Recognized security domain implication: input is silent on handling of decrypted key material in process memory (e.g. zeroing derived secrets, avoiding swap) despite this being a cold-storage security tool for highly sensitive material
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS. All 7 capabilities carry intent+success; intents are WHAT not HOW; all 7 constraints each rule something out; 2 non-goals present; success signal is a concrete demonstrable scenario; capability IDs unique and stable; prose lean
- (event) Self-validate pass 2 (preservation): PASS. All claims from brainstorm-intent.md accounted for - Problem/Motivation into Why, 7 Must-Haves into CAP-1..7, 2 Should-Haves folded into Constraints (logged as assumption), 6 Non-Negotiable Constraints preserved verbatim in intent, 2 Out-of-Scope items into Non-goals. No wrapper-only content dropped
- (event) spec finalized: SPEC-tomb-fido2, 7 capabilities, 7 constraints, 2 non-goals, 1 assumption, 2 open questions, 0 companions
- (decision by user) Resolves open question on last-keyslot revocation: tomb-fido2 must actively prevent revoking the last remaining valid keyslot, a deliberate departure from raw cryptsetup (which permits self-lockout). Promoted to a Constraint and folded into CAP-3's success criterion
- (decision by user) Resolves open question on in-memory secret handling: tool must make a best-effort attempt to avoid decrypted key material leaking/persisting in process memory. User is not the authority on the specific mechanism (zeroing strategy, mlock, etc.) - deferred to the architect as an implementation decision. Promoted to a Constraint stating the WHAT (best-effort secret hygiene), not the HOW
- (event) spec re-derived after resolving both open questions: CAP-3 success criterion now includes last-keyslot guard; 2 new constraints added (last-keyslot protection, best-effort in-memory secret hygiene deferred to architect); open questions cleared to 0. Re-checked rules 1 and 3 - PASS
- (direction) User direction: add LUKS2 volume management (creation, resizing, mounting, unmounting) as in-scope capabilities. Confirmed as a genuine gap, not an intentional exclusion - managing volumes is a core strength of the original dyne/tomb this project reimagines and the user wants to retain it
- (decision) User decision: mount/unmount are bundled with unlock/lock, not exposed as independent capabilities - preserves dyne/tomb's open/close UX model
- (decision) User decision: the first FIDO2 key's enrollment is bootstrapped as part of tomb creation, not a separate manual step. CAP-2 (enroll) remains scoped to additional keys on an already-created tomb, unchanged
- (decision) User decision: resize is grow-only for v1; shrinking an existing tomb is out of scope
- (decision) User decision: filesystem type is user-selectable at tomb creation time, not a fixed default
- (decision) Amend CAP-1 (unlock): success criterion now includes the volume's filesystem being mounted and accessible, not only the LUKS2 device being unlocked, since mount is bundled with unlock per user direction
- (decision) Amend CAP-4 (unified CLI): scope now also covers create/lock/resize (and their bundled mount/unmount), replacing additional direct invocations of mkfs/mount/umount/cryptsetup-resize, not only unlock/enroll/revoke/preflight
- (capability) CAP-8 intent: user can create a brand-new tomb - format a backing file or raw partition/block device as LUKS2, create a user-selected filesystem inside it, and bootstrap-enroll the first FIDO2 key, all as one operation. success: after creation, the new tomb unlocks and mounts successfully (CAP-1) using the newly enrolled key, with the chosen filesystem ready to use
- (capability) CAP-9 intent: user can close an unlocked tomb - unmount its filesystem and re-lock the LUKS2 volume - as the symmetric counterpart to CAP-1's unlock+mount. success: after closing, the mount point is no longer accessible and the volume requires a FIDO2 key to unlock again
- (capability) CAP-10 intent: user can grow an existing tomb's LUKS2 volume and filesystem to a larger size without recreating it or re-enrolling any FIDO2 keys. success: after growing, the volume's usable capacity reflects the new size, all previously enrolled FIDO2 keys still unlock it, and no existing data is lost
- (constraint) Filesystem operations (mkfs at creation, growfs at resize, mount/unmount) use only standard, well-known tools/syscalls for the user-selected filesystem type - rules out any custom or tomb-fido2-proprietary filesystem handling
- (constraint) Amend break-glass constraint: README must also document manual mount/unmount (in addition to unlock) using only bare cryptsetup + mount/umount commands, since mount is now bundled into the tool's unlock/lock workflow
- (non-goal) Shrinking an existing tomb is out of scope for v1 - resize is grow-only
- (decision) Amend CAP-7 (raw device + loop file support): generalized from unlock/enroll/revoke to all tomb-fido2 operations (create, unlock+mount, enroll, revoke, close+unmount, resize) - the new create/lock/resize capabilities inherit the same raw-partition-or-loop-file flexibility, consistent with CAP-7's original intent
- (event) Self-validate pass 1 (coherence, rules 1-6+8) after volume-management addition: PASS. All 10 capabilities carry intent+success; intents remain WHAT-level; all constraints rule something out; 3 non-goals present; success signal covers full lifecycle (create/unlock+mount/close+unmount/resize) and remains demonstrable; capability IDs CAP-1..10 unique and stable (no renumbering, no reuse); prose lean
- (event) Self-validate pass 2 (preservation) after volume-management addition: PASS. All 4 user decisions (bundle mount/unmount with unlock/lock, bootstrap-enroll at creation, grow-only resize, user-selectable filesystem) landed in CAP-1 (amended), CAP-4 (amended), CAP-7 (amended), CAP-8, CAP-9, CAP-10, plus 1 new constraint and 1 new non-goal. No wrapper-only content dropped
- (event) SPEC re-derived: spec-tomb-fido2 now carries 10 capabilities (CAP-1..10), 10 constraints, 3 non-goals, 1 assumption, 0 open questions, 0 companions. Downstream Architecture Spine (architecture-tomb-fido2-2026-07-22) and the in-progress epics.md draft both predate this change and need re-validation/rework
- (decision) User decision: read-only unlock uses block-device-level read-only (cryptsetup luksOpen --readonly) in addition to a read-only filesystem mount, not just a filesystem-level mount -o ro like original dyne/tomb - chosen to match tomb-fido2's stronger cold-storage safety ethos (already exceeds original tomb via raw device support and the last-keyslot guard)
- (capability) CAP-11 intent: user can unlock and mount an existing tomb in read-only mode, so the LUKS2/dm-crypt mapping itself refuses writes at the block-device level, not merely the filesystem mount. success: when read-only mode is requested, both the underlying mapper device and the mounted filesystem reject write attempts (including a later remount attempt), while a normal (non-read-only) unlock continues to allow writes as before
- (constraint) Read-only unlock (CAP-11) must refuse writes at both the LUKS2/dm-crypt mapping level and the filesystem mount level - a filesystem-level-only read-only mount (plain mount -o ro over a read-write dm-crypt mapping) does not satisfy this capability
- (event) Self-validate pass 1 (coherence, rules 1-6+8) after read-only capability addition: PASS. CAP-11 carries intent+success; intent is WHAT-level (read-only unlock, not HOW it's implemented beyond the block-device-vs-mount distinction the user explicitly decided); new constraint rules out the easier filesystem-only implementation; capability IDs remain unique/stable (CAP-1..11, no reuse); prose lean
- (event) Self-validate pass 2 (preservation) after read-only capability addition: PASS. User decision (block-device-level read-only, stronger than original tomb) landed in CAP-11 and its dedicated constraint. No wrapper-only content dropped
- (event) SPEC re-derived: spec-tomb-fido2 now carries 11 capabilities (CAP-1..11), 11 constraints, 3 non-goals, 1 assumption, 0 open questions, 0 companions. Downstream Architecture Spine and epics.md draft still pending rework from the earlier volume-management addition, now also need to account for CAP-11 (read-only unlock)
- (direction by user) User direction: CAP-8 (create) was ambiguous about requiring a pre-existing backing file. Desired behavior mirrors dyne/tomb's 'tomb dig': for a file-backed tomb, user supplies only a destination path and a size, and the tool allocates the backing file itself as part of create - the user must never manually pre-create a backing file (e.g. via dd/fallocate/truncate).
- (decision by user) User decision: create supports two target modes with different size semantics - (a) file-backed: destination path + explicit size, tool allocates the backing file at that size; (b) device-backed: an existing raw block device/partition path, size argument is not applicable - the tool always uses the device/partition's full capacity, never a user-supplied size.
- (constraint) Creating a file-backed tomb never requires a pre-existing backing file - the tool allocates it at the given destination path and size as part of create (tomb-dig-style), ruling out any workflow that expects the user to run dd/fallocate/truncate manually beforehand.
- (constraint) Creating a device-backed tomb always occupies the target device/partition's full capacity - a user-supplied size argument is not applicable to device targets and must not silently truncate usable capacity to a smaller size.
- (question) Input doesn't address whether create refuses when the destination already holds data - an existing non-empty file at the file-backed destination path, or a device/partition that already carries a filesystem/LUKS header - a real data-loss risk parallel to the earlier last-keyslot lockout gap. Needs a decision before CAP-8 is fully implementation-ready.
- (decision by user) Amend CAP-8 (create) and CAP-7 (raw device support): CAP-8 intent/success now explicitly cover both create target modes (file-backed with tool-allocated backing file at a given size; device-backed using full device/partition capacity, size not applicable). CAP-7 already generalized create to raw devices; CAP-8 now spells out the full-capacity semantics for that case.
- (direction by user) User direction: 'tomb-fido2' is a placeholder project name only - a permanent, catchier name is still pending and will change later. The current name must not be baked into the overall wording or the implementation (CLI/binary/package name, user-facing strings, on-disk metadata field names).
- (constraint) Product name 'tomb-fido2' is a placeholder, not final branding - CLI/binary name, package/module name, user-facing strings, and on-disk metadata field names must not hardcode or assume permanence of this name. This SPEC's own prose refers to the product generically ('the tool', 'a tomb') rather than by the placeholder, so a future rename requires no SPEC rework.
- (event) Self-validate pass 1 (coherence, rules 1-6+8) after create/raw-device/naming amendment: PASS. CAP-8 intent now spells out both target modes (file-backed allocate-then-format; device-backed full-capacity) as WHAT, not HOW; 3 new constraints each rule something out (no pre-existing backing file, device size not applicable, placeholder-name non-hardcoding); capability IDs unchanged/stable (CAP-1..11); prose stays lean; 1 new open question logged rather than invented.
- (event) Self-validate pass 2 (preservation) after create/raw-device/naming amendment: PASS. All 3 user asks landed - (1) tool-allocated backing file given destination+size into CAP-8 + new constraint, (2) device-backed create using full partition capacity into CAP-8 + new constraint, (3) 'tomb-fido2' avoided in SPEC prose (title, Why, CAP-1, constraints, success signal reworded to generic 'the tool'/'a tomb') plus a new constraint extending this to the implementation. No wrapper-only content dropped.
- (event) SPEC re-derived: spec-tomb-fido2 now carries 11 capabilities (CAP-1..11, CAP-8 amended), 14 constraints (3 new: no-pre-existing-backing-file, device-full-capacity, placeholder-name), 3 non-goals, 1 assumption, 1 open question (destination-already-has-data overwrite safety). Downstream Architecture Spine (architecture-tomb-fido2-2026-07-22) and epics.md both predate this change and need rework: (a) AD-9's create flow assumed a size/pre-existing-file model not yet reflecting tool-allocated file vs full-device-capacity branching, (b) both documents embed the placeholder name into identifiers (tomb_fido2_label, tombfido2- mapping prefix, doc titles, FR/NFR text) that the new naming constraint says should not be hardcoded.
- (decision by user) Resolves open question on destination overwrite: create refuses outright (no proceeding) if the file-backed destination path already exists, or if the device-backed target already carries an existing LUKS2 header - never silently overwrites either.
- (decision by user) New device-specific safeguard: for any device-backed create - regardless of whether an existing LUKS2 header was detected - the tool must interactively prompt the user for explicit confirmation, showing a clear warning about the impending wipe and irreversible data loss, before formatting. Rationale: picking the wrong device (e.g. /dev/sdc vs /dev/sdb) is a distinct, higher-stakes risk than a typo'd file path, which the hard refuse alone doesn't cover.
- (decision by user) Reverses the prior-turn device-full-capacity-only decision (memlog entry ~52-56): user confirms this was already discussed with John (PM) previously. --size applies to device-backed create too - optional, may be set smaller than the device/partition's actual capacity to leave free space for a later CAP-10 grow; if omitted, defaults to the device's full capacity; must never exceed the device's actual capacity.
- (event) Self-validate pass 1 (coherence, rules 1-6+8) after overwrite-refusal/device-confirmation/size-reversal amendment: PASS. CAP-8 intent+success cover both target modes with refusal + confirmation semantics as WHAT (no HOW prescribed for the confirmation UI itself); 2 new constraints each rule something out (hard refuse over silent overwrite, mandatory device wipe-warning even without a detected header); the full-capacity constraint corrected to allow an optional smaller size, still ruling out exceeding actual capacity; capability IDs unchanged/stable (CAP-1..11); prose lean; open question resolved and removed rather than left dangling.
- (event) Self-validate pass 2 (preservation) after overwrite-refusal/device-confirmation/size-reversal amendment: PASS. All 3 user decisions landed - (1) hard refuse on existing file-backed destination or existing LUKS2 header on device targets, into CAP-8 + new constraint, (2) mandatory interactive wipe/data-loss confirmation for any device-backed create, into CAP-8 + new constraint, (3) --size restored as optional-with-full-capacity-default for device targets (reversing the prior turn), into CAP-8 + corrected constraint. No wrapper-only content dropped.
- (event) SPEC re-derived: spec-tomb-fido2 now carries 11 capabilities (CAP-1..11, CAP-8 amended again), 15 constraints (net +1 vs previous render: +2 new overwrite/confirmation constraints, 1 corrected in place for device sizing), 3 non-goals, 1 assumption, 0 open questions, 0 companions. Downstream Architecture Spine, epics.md, and README.md remain stale against both this and the prior create/naming amendment - still need rework covering: two create target-mode branches with refusal + confirmation UX, device --size semantics (optional, defaults full capacity, headroom for CAP-10), and the placeholder-name non-hardcoding constraint.
- (decision) User (via John's review during epics/stories creation) flagged that SPEC.md's placeholder-name callout (line 11) should match the clarifying tail already added to epics.md's mirrored callout - append 'Referred to as "the tool" throughout this document.' No prose beyond the two intentional placeholder-name mentions (title callout, naming constraint) needs further change; SPEC.md already uses generic language elsewhere.
- (event) SPEC re-derived: added clarifying tail to line-11 placeholder-name callout (now matches epics.md wording). No capability/constraint/non-goal content changed - 11 capabilities, 15 constraints, 3 non-goals, 1 assumption, 0 open questions, 0 companions, unchanged.
- (note) source: _bmad-output/brainstorming/brainstorm-tomb-fido2-2026-07-27/brainstorm-intent-epic4.md, Epic 4 candidate features post-Epic-3-retro
- (decision by user) Info/inspection capability is one general 'info' command (technical info about a tomb) with enrolled FIDO2 keys as one section, not a dedicated 'list-keys' command
- (note) info command is read-only and does not require unlock first - list_fido2_keyslots/token metadata already read directly off the LUKS2 header by existing enroll/revoke code, confirmed against ARCHITECTURE-SPINE.md ports (list_fido2_keyslots)
- (decision by user) FIDO2 user-verification (fingerprint/PIN) support: enroll with --fido2-with-user-verification=true, available at both create's bootstrap enrollment and standalone enroll; UV is a stronger verification mode of the existing FIDO2 mechanism, not a new auth path, so it does not violate the no-fallback-auth-paths constraint
- (question) Whether unlock/resize's existing token-based open call needs any change to support a UV-enrolled key, or cryptsetup's token machinery already handles it transparently, is an implementation question deferred to the architecture step - not decided here, does not block CAP definition
- (decision by user) 'close all' closes every currently-open/unlocked tomb in one command
- (decision by user) 'slam' fires immediately with no confirmation prompt, unlike create's wipe warning or revoke's last-keyslot guard - user explicitly chose no-confirmation to preserve slam's emergency/panic-button framing
- (decision by user) slam's process-killing mechanism matches dyne/tomb's real implementation (tomb#L3391-3406, fetched from source not memory): identify processes holding the tomb's mountpoint via lsof, escalate signals TERM -> HUP -> KILL against any remaining processes each round with a brief pause between rounds, stop early once none remain
- (question) How the tool discovers 'all currently open tombs' (no registry exists per existing no-side-channel-state posture; likely live-enumeration of active dm-crypt mappings) is an implementation mechanism deferred to the architecture step - not decided here, does not block CAP definition
- (constraint) close-all/slam must discover open tombs by live-querying system state only, never a stored registry/lock file - generalizes the existing no-side-channel-state architecture posture (AD-2) into an explicit SPEC constraint now that product-level capabilities (CAP-14/15) directly depend on it
- (decision by user) Hooks lifecycle scope for v1: open/close only, matching dyne/tomb parity - not extended to create/resize/read-only-unlock, to limit scope on what's already the largest architectural departure this epic introduces
- (decision by user) exec-hooks always run as the invoking (non-root) user, never with elevated privilege, regardless of what privilege the lifecycle step itself needs (e.g. close's sudo unmount) - matches dyne/tomb precedent and codebase's existing least-privilege posture
- (decision by user) Hook config (bind-hooks, exec-hooks files) lives per-tomb in the tomb's own root, matching dyne/tomb exactly - keeps hook config traveling with the tomb, break-glass friendly, consistent with no-side-channel-state posture
- (note) Compared dyne/tomb's actual hook guardrails (tomb#L2824-2910, fetched from source): exec-hooks only checks executable bit (passes symlinks), bind-hooks only checks file exists + two-field format (no ownership check anywhere, no path-containment check on bind-hooks source/destination)
- (decision by user) Add stricter guardrails beyond dyne/tomb's stock model: exec-hooks must be a regular file (reject symlinks) with the executable bit set; hook files must be owned by the invoking user or root and not world-writable; bind-hooks entries must resolve within the tomb root (source) and within $HOME (destination) - no path traversal via .. or absolute escape
- (decision by user) Resolve progress-reporting now for Epic 4 (deferred at every epic boundary since Story 1.5, 2026-07-23) rather than defer a 4th time - create and resize get real step-by-step named-stage progress messages (e.g. allocating, formatting as LUKS2, creating filesystem, enrolling FIDO2 key for create; resizing LUKS2 mapping, growing filesystem for resize), replacing the Story 1.5 stopgap of one message before and one after the whole operation
- (decision) Reconfirmed: existing Non-goals (remote/delegated unlock beyond cryptsetup's native token mode, post-quantum-readiness, shrink) still hold - none of the Epic 4 candidate features touch them
- (capability) CAP-12 intent: user can view technical info about a tomb, including its currently enrolled FIDO2 keys with their labels, through one info command, without needing to unlock it first. success: running the info command against a tomb shows all currently enrolled FIDO2 keys with their labels, and this works without running unlock first
- (capability) CAP-13 intent: user can enroll a FIDO2 key with user-verification (fingerprint/PIN) required instead of touch-only presence, both at tomb creation's bootstrap enrollment and via the standalone enroll command. success: a key enrolled with user-verification requires the device's own fingerprint/PIN check to unlock the tomb, not just a touch; a key enrolled without it continues to unlock with touch alone, unchanged
- (capability) CAP-14 intent: user can close every currently-open/unlocked tomb in one command. success: after running close-all, no tomb-fido2-managed mapping remains open, and each affected tomb's volume requires a FIDO2 key to unlock again - the same end-state as running close once per open tomb
- (capability) CAP-15 intent: user can run an emergency command that closes every open tomb and forcibly clears any process holding a mount busy, firing immediately with no confirmation prompt. success: slam closes every open tomb; for any mount blocked by a busy process it escalates signals (TERM, then HUP, then KILL) against the processes holding that mount, pausing briefly between rounds and stopping once none remain, and it never pauses for confirmation before acting
- (capability) CAP-16 intent: user can define per-tomb bind-hooks (auto bind-mount tomb-internal paths onto $HOME-relative paths on open) and an exec-hooks executable (run as the invoking user at open/close), both similar to dyne/tomb's model but not necessarily an exact port, with an option to skip hook processing for a given invocation. success: on open, each valid bind-hooks mapping is bind-mounted and each exec-hooks file runs with 'open' plus the mountpoint as arguments; on close, exec-hooks runs with 'close' plus the mountpoint, tomb name, loopback device, and mapper device as arguments; bind-hooks entries that fail the path-containment or existence checks are skipped with a warning, not silently applied; invoking with the skip option runs neither mechanism
- (capability) CAP-17 intent: user gets real step-by-step progress messages during create and resize, naming each real stage as it begins and completes, replacing the Story 1.5 stopgap of one message before and one after the whole operation. success: running create or resize prints a distinct message for each real stage of that operation in order (e.g. allocating, formatting as LUKS2, creating the filesystem, enrolling the FIDO2 key for create; resizing the LUKS2 mapping, growing the filesystem for resize), not just a single start/end message
- (constraint) UV enrollment (CAP-13) is a stronger verification mode of the existing FIDO2 mechanism, not a new auth path - does not violate the no-fallback-auth-paths constraint
- (constraint) Hooks (CAP-16) introduce a new risk category (arbitrary user-authored code execution) this SPEC has not previously reasoned about: hooks run only at open/close (not create/resize/read-only-unlock); exec-hooks always runs as the invoking user, never with elevated privilege; hook files live per-tomb in the tomb's own root; exec-hooks must be a regular file (not a symlink) with the executable bit set, owned by the invoking user or root, and not world-writable; bind-hooks entries must resolve within the tomb root (source) and within $HOME (destination), rejecting path traversal; an invocation-level option to skip hook processing entirely must exist
- (constraint) Create and resize must report distinct named-stage progress messages as each real stage occurs (CAP-17), not merely a single message before and after the whole operation - supersedes the Story 1.5 stopgap
- (decision) Hooks mechanism detail (file formats, argument lists, disable option, guardrail specifics) spawned into a new spec-authored companion, hooks.md, since it is multi-line reference/catalog content that would bloat the kernel - the largest single addition of this update
- (event) Self-validate pass 1 (coherence, rules 1-6+8) after Epic 4 candidate-feature addition: PASS. CAP-12..17 each carry intent+success; intents stay WHAT-level (info command's read-only-without-unlock is an existing behavior being surfaced, not new HOW; slam's kill mechanism is stated as observable escalation behavior, matching the real dyne/tomb precedent the user confirmed, not arbitrary internal HOW; hooks' argument lists and file formats are user-facing contract, not implementation detail); new constraints each rule something out (no stored registry for close-all/slam, hooks guardrails, named-stage progress); non-goals unchanged (3, reconfirmed); success signal needs re-derivation to cover the fuller lifecycle; capability IDs CAP-12..17 unique, stable, no renumbering of CAP-1..11; prose lean
- (event) Self-validate pass 2 (preservation) after Epic 4 candidate-feature addition: PASS. All Must-Have items from brainstorm-intent-epic4.md landed - info/key-inspection into CAP-12, UV support into CAP-13, close-all into CAP-14, slam into CAP-15 (with real dyne/tomb signal-escalation mechanism substituted for the brainstorm's open question, per user-provided source), bind-hooks/exec-hooks into CAP-16 + hooks.md companion, carried-over progress-reporting item into CAP-17. Architecture-only open items (UV/open-call interaction, open-tomb discovery mechanism) intentionally left unresolved here and logged as questions for the architecture step, not invented. Non-Negotiable Design Constraints section reconfirmed against existing SPEC constraints (CAP-4 extension, no-fallback-auth via UV, no-side-channel-state via new close-all/slam constraint, new hooks risk-category constraint) - none silently dropped. Non-goals reconfirmed unchanged. No wrapper-only content dropped
- (event) SPEC re-derived: spec-tomb-fido2 now carries 17 capabilities (CAP-1..17, CAP-1..11 unchanged), 20 constraints (5 new: UV-not-a-new-authpath, close-all/slam no-registry, slam no-confirmation, hooks risk-category+guardrails, progress-reporting stages), 3 non-goals (reconfirmed unchanged), 3 assumptions (1 new: two architecture-deferred mechanism questions), 0 open questions, 1 companion (hooks.md, new). sources now includes brainstorm-intent-epic4.md (fully absorbed)
- (decision) Preservation-pass catch: amended CAP-4 (unified CLI) intent to explicitly list the new commands (UV enrollment, close-all, slam, info) alongside the existing ones, per brainstorm's own note that CAP-4 extends naturally to them - CAP-4 success criterion unchanged
- (event) Self-validate re-check after CAP-4 amendment: PASS, rules 1/2/6/8 unaffected (intent still WHAT-level command enumeration, ID/success unchanged). SPEC.md and hooks.md now final for this session: 17 capabilities, 20 constraints, 3 non-goals, 3 assumptions, 0 open questions, 1 companion
- (decision) ARCHITECTURE-SPINE.md (v4, ../../planning-artifacts/architecture/architecture-tomb-fido2-2026-07-22/ARCHITECTURE-SPINE.md, AD-1..19, binds CAP-1..17) adopted as a companion alongside hooks.md, per user request following Winston's Epic 4 architecture update. Adopted, not spec-authored -- bmad-architecture owns and edits it; bmad-spec references it into companions: only.
- (event) SPEC.md frontmatter companions: updated from [hooks.md] to [hooks.md, ../../planning-artifacts/architecture/architecture-tomb-fido2-2026-07-22/ARCHITECTURE-SPINE.md]. No kernel content changed -- this is a companions-list-only update.
- (event) Self-validate: companions-list-only change, no kernel field touched -- Spec Law 1-6/8 unaffected. Preservation N/A (adopted companion, not absorbed source content; hooks.md remains the only spec-authored/adopted companion whose content is cited from the kernel). Verdict: clean.
- (note) source: _bmad-output/planning-artifacts/epics.md 'Backlog — Unscoped Candidate Ideas' section (entries captured 2026-08-02 through 2026-08-07), reviewed 2026-08-07 to scope candidates for a new Epic 6
- (capability) CAP-18 intent: user can supply a custom label for the first FIDO2 key enrolled during create's bootstrap step (create.rs currently hardcodes 'primary'), mirroring enroll's existing required --label flag. success: create accepts an optional --label; when given, the tomb's key_label metadata equals it; when omitted, falls back to today's 'primary' default
- (capability) CAP-19 intent: user can optionally have create scaffold example bind-hooks/exec-hooks template files into a new volume, so hooks (CAP-16) are discoverable without consulting docs first. success: when requested, create writes commented-out example bind-hooks entries and a non-executable exec-hooks stub into the volume root; when not requested (default), no template files are written
- (assumption) CAP-19 template scaffolding is opt-in via a flag, default off — avoids surprising users with unrequested files in their volume; not directly confirmed, inferred from 'optionally' in the backlog wording
- (capability) CAP-20 intent: user can use one-letter shorthand aliases for each subcommand's primary flags across the whole CLI (today every flag is --long only, confirmed via src/cli/main.rs). success: --help for every subcommand documents a short alias for its options; each alias behaves identically to its long form
- (capability) CAP-21 intent: contributors/users see repo-hygiene badges (build status, license, latest release, MSRV) in the README header at a glance, plus coverage and security-audit badges once their underlying instrumentation exists. success: README header shows build-status/license(GPL-3.0-or-later)/latest-release/MSRV badges, each linking to the resource it reflects; coverage and security-audit badges land in a follow-up once open questions below are resolved
- (question) CAP-21 coverage badge: which coverage tool (cargo-llvm-cov vs tarpaulin) and hosting service (Codecov vs Coveralls)? No instrumentation exists yet — needs a decision before scoping
- (question) CAP-21 security-audit badge: which tool for the new CI job (cargo-audit vs cargo-deny), and does it gate CI (fail the build) or just report/badge? Flagged as more relevant than coverage for a crypto/security tool
- (capability) CAP-22 intent: user can create/resize a tomb using XFS or Btrfs as the filesystem, in addition to today's ext4-only support, via the existing additive FilesystemBackend port design (AD-8, confirmed only Filesystem::Ext4 exists today). success: --filesystem xfs/btrfs accepted by create; resize's growfs step uses the matching tool (xfs_growfs, btrfs filesystem resize) read from the same filesystem token field; break-glass recoverability (existing Constraints) holds identically to ext4
- (non-goal) 'Mixed mode' filesystem support (per dyne/tomb's model — running different filesystems within one volume, or whatever that turns out to mean) stays out of scope for this pass; scope is undecided and deferred pending its own future elicitation, distinct from CAP-22's per-volume single-filesystem-type XFS/Btrfs support
- (capability) CAP-23 intent: user can re-run create against the same destination after a crash/interruption during a prior create attempt, and have the tool complete or cleanly restart it, rather than being left with an unrecoverable partial tomb (AD-9's known gap)
- (capability) CAP-23 success: re-running create after a simulated mid-bootstrap interruption against the same destination results in the same fully-created, unlockable tomb as an uninterrupted run — no manual cleanup (deleting a half-formatted file/header) required first
- (question) CAP-23 tensions directly with the existing constraint that create refuses outright if a file-backed destination already exists or a device-backed target already carries a LUKS2 header — how does create distinguish 'a partial tomb safe to resume' from 'a genuine pre-existing file/volume it must refuse', and does resume continue the original attempt or restart clean? Needs a human decision before CAP-23 is implementation-ready
- (capability) CAP-24 intent: tool prevents two simultaneous invocations against the same tomb from racing past each other in a way that corrupts state or bypasses a safety guard, particularly AD-5's live-count last-keyslot guard (TOCTOU risk noted as a SPEC non-goal previously, deferred as low-likelihood for a single-user tool). success: running two revoke invocations concurrently against a tomb with exactly two valid keyslots, each targeting a different key, never results in both succeeding (which would leave zero valid keyslots) — the second invocation either serializes behind the first or fails cleanly with a clear 'another operation is in progress' error
- (capability) CAP-25 intent: tool gives plain-language guidance when the FIDO2 device involved in an operation has a device-level PIN configured (fido2-token -S -i), consistent with CAP-5's zero-FIDO2-knowledge posture — distinct from user-verification (CAP-13), which cryptsetup's systemd-fido2 plugin already reads automatically at unlock with no code change needed. success: enrolling or unlocking with a PIN-required device shows a clear prompt naming that PIN entry is expected before the touch step; a wrong-PIN retry shows a plain-language warning naming that retries are limited and what happens if they run out, never a generic 'authentication failed'
- (question) CAP-25: proactive PIN-required detection (preflight/enumeration, warns before the user attempts anything) vs reactive translation of the subprocess failure after the fact (simpler, no new port method) — undecided, needs a human/architecture decision
- (question) Should the CLI expose the full set of systemd-cryptenroll FIDO2 flags (--fido2-credential-algorithm, --fido2-salt-file, --fido2-parameters-in-header, --fido2-with-client-pin, --fido2-with-user-presence) to advanced users during enrollment, weighed against the zero-fallback/zero-cognitive-overhead design posture (NFR3/NFR5)? Not scoped as a capability until this is decided — no CAP-N assigned
- (decision) Publishing to crates.io remains declined (reaffirms the 2026-08-06 decision recorded in epics.md, rationale: hypogaol shells out to system binaries cargo install can't provide; would be a second release surface to keep in sync with cargo-dist+release-please; falls inside AR-Dev4's existing packaging scope fence) — not part of Epic 6 scope, no capability spawned
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS for CAP-18..25 — each carries intent+success; intents are WHAT not HOW; new IDs unique/sequential continuing from CAP-17; prose lean. CAP-19 and CAP-24's HOW-shaped mechanism questions correctly deferred to architecture, not left implicit in the capability text
- (event) Self-validate pass 2 (preservation): PASS. All 10 backlog items from epics.md accounted for: 8 promoted to CAP-18..25, 1 (cryptenroll flag exposure) held as an unscoped open question per the backlog's own framing, 1 (crates.io) recorded as a reaffirmed decision, not a capability. No wrapper-only content dropped
- (event) spec updated: SPEC-tomb-fido2 now 25 capabilities (18 new: CAP-18..25), unchanged constraints, 4 non-goals (1 new: mixed-mode filesystem support), unchanged success signal, 2 assumptions total (1 new: CAP-19 opt-in default), 5 open questions (all new), 0 companions added — ready for a human pass on the open questions, then bmad-create-epics-and-stories to scope Epic 6
- (decision by user) Btrfs 'mixed mode' is folded into CAP-22 rather than deferred as a non-goal — it's just mkfs.btrfs's --mixed flag (data+metadata share one block group), which drops Btrfs's minimum viable volume size from ~114MB to ~18MB; not a per-volume multi-filesystem scheme as the backlog's dyne/tomb comparison implied. Supersedes the mixed-mode non-goal entry
- (decision by user) CAP-21 coverage badge: cargo-llvm-cov (instrumentation) + Codecov (hosting) — resolves open question 1
- (decision by user) CAP-21 security-audit badge: cargo-audit, gating CI (fails the build on a known RustSec advisory, not merely a badge) — resolves open question 2
- (decision by user) CAP-23 (resume crashed create): restart-clean, not incremental resume. Detection via a hypogaol-owned custom LUKS2 token type (empty keyslots array, inert at unlock, same mechanism AD-2 already uses for key_label/filesystem metadata) written immediately after luksFormat succeeds and removed as the last step of create, alongside the existing bootstrap-keyslot cleanup. A LUKS2 header found WITH this marker token means a resumable partial attempt (wipe + restart create clean); a header found WITHOUT it means a genuine pre-existing volume (refuse, unchanged). Resolves open question 3
- (decision by user) CAP-25 (PIN-required device UX): proactive detection — query the connected authenticator (fido2-token -L/-I) for clientPin status during preflight/device enumeration, warn before the touch prompt, rather than only translating the subprocess failure after the fact. Chosen over reactive-only to keep the UX as clear and simple as possible under crisis stress, consistent with CAP-5/NFR3. Resolves open question 4
- (decision by user) Exposing the full systemd-cryptenroll FIDO2 flag set: deferred, not declined. User's reasoning: if exposed, these flags would be fully optional/opt-in (mirroring systemd-cryptenroll itself) — a non-technical user in crisis never sees or needs them, so this does not actually conflict with NFR3/NFR5 the way blanket exposure would. Deferred anyway because the need is fully speculative — no concrete user or use case has hit this wall yet, not even the user himself. Revisit if a real need surfaces. Resolves open question 5
- (event) All 5 open questions from the Epic 6 backlog review resolved via user walkthrough: (1) coverage badge = cargo-llvm-cov + Codecov, (2) security-audit badge = cargo-audit gating CI, (3) CAP-23 resume = hypogaol-owned LUKS2 marker token, restart-clean, (4) CAP-25 PIN UX = proactive detection, (5) full cryptenroll flag exposure = deferred (not declined) as a non-goal, user's opt-in-doesn't-violate-NFR3/NFR5 reasoning preserved verbatim
- (event) Self-validate re-run (rules 1-8): PASS. 25 capabilities all carry intent+success; CAP-22 now covers Btrfs mixed mode (user correction: it's mkfs.btrfs --mixed, not a multi-filesystem scheme, folded in rather than kept as a non-goal); CAP-23's constraint collision resolved with an explicit exception clause on the pre-existing constraint rather than a silent rewrite; 4 non-goals (1 new: deferred cryptenroll flag exposure, with rationale preserved); Open Questions section empty and correctly omitted. Spec is Epic-6-ready
- (decision) Amended the device-backed confirmation constraint (line 135) with a CAP-23 marker-token exception clause, mirroring the sibling refuse-constraint's (line 134) existing exception -- previously only the refuse-constraint had been amended, leaving the confirmation-constraint in literal conflict with the architecture spine's AD-9 decision (marker-verified resume skips the confirmation prompt). Surfaced by the architecture spine's reconcile-inputs reviewer during Epic 6 architecture work (2026-08-08) and resolved by user decision: amend SPEC.md rather than reverse the architecture decision.
- (event) Refresh sweep (bmad-spec, post-Epic-6-architecture): found two stale Assumptions-section entries. (1) The Epic-4-era UV-enrollment/close-all-enumeration mechanism assumption was already resolved by the architecture spine's AD-16/AD-17 back on 2026-07-27 -- never swept from SPEC.md's Assumptions section at the time. (2) CAP-19's opt-in-default assumption is now explicitly confirmed (user agreed during Epic 6 architecture coaching, 2026-08-08) and is redundant with CAP-19's own success text, which already states the default. Both removed from Assumptions on this derive; no downstream consumer loses information, since AD-16/AD-17 and CAP-19's success text already carry the content.
- (event) Self-validate pass 1 (coherence, rules 1-6+8) on refresh: PASS. 25 capabilities unchanged (all still carry intent+success); IDs stable; 4 non-goals; success signal unchanged and still concrete; the amended device-backed confirmation constraint still bends a real design decision (now with the CAP-23 marker exception); Assumptions section leaner after removing two stale/resolved entries.
- (event) Self-validate pass 2 (preservation) on refresh: PASS. No new source content this pass (refresh/amendment only, not a new distillation) -- confirmed the two removed Assumptions-section entries drop nothing load-bearing: the UV-enrollment/close-all-enumeration mechanism content already lives in the architecture spine companion's AD-16/AD-17, and CAP-19's opt-in-default behavior already lives in CAP-19's own success text. companions: unchanged [hooks.md, architecture spine]; sources: unchanged. Spec refreshed and consistent with the amended architecture spine.
