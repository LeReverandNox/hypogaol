---
topic: tomb-fido2 architecture spine
scope: Whole tomb-fido2 CLI: unlock, enroll, revoke, dependency check against LUKS2 + FIDO2 hmac-secret
purpose: build-substrate spine + public-facing README as companion deliverable
altitude: initiative
updated: 2026-07-22T15:46
---

- (direction) User chose Coaching path over Fast path
- (direction) Deliverable beyond the spine: a public-facing README that doubles as project documentation, since the project is intended to go public eventually
- (note) Reality check: local system has systemd 261 (+FIDO2, +LIBCRYPTSETUP_PLUGINS) and cryptsetup 2.8.6 — confirms systemd-cryptenroll + cryptsetup's built-in FIDO2 token plugin path is viable and current, not a stale assumption
- (decision) Process boundary: tomb-fido2 is a thin orchestrator over systemd-cryptenroll (enroll), cryptsetup (open/close/luksRemoveKey/token ops), and fido2-token (device listing, pre-flight checks). It does NOT reimplement CTAP2/hmac-secret or LUKS2 keyslot crypto itself — that stays inside cryptsetup's own maintained FIDO2 token plugin. Binds: CAP-1..7. Prevents: reimplementing security-sensitive crypto protocols from scratch; keeps break-glass recoverability trivially true since the binary is genuinely just a wrapper. Bonus: strengthens the memory-hygiene constraint for free since the actual LUKS passphrase/secret is derived+consumed inside cryptsetup's process, never tomb-fido2's, for the unlock path.
- (decision) Language/stack: Rust. Rationale given by user: fits the 'wrapper around other tools' shape and the zeroize/security-tool ecosystem convention (age, rage, sequoia-pgp); Go would have been the pick only if reimplementing crypto ourselves, which we're not (AD-1).
- (direction) User wants well-organized, nicely factorized code with clear abstraction boundaries between parts, tightly scoped — explicit ask for a seam between backend-tool orchestration and CLI/UX layer, so guardrail logic (e.g. last-keyslot block) isn't tangled with process-exec plumbing.
- (decision) Paradigm: Hexagonal / Ports & Adapters. domain/ = pure policy (unlock/enroll/revoke workflows + guardrails, e.g. last-keyslot block), ports/ = traits (LuksBackend, Fido2Backend), adapters/exec/ = real shell-out implementation parsing cryptsetup/systemd-cryptenroll/fido2-token output, cli/ = arg parsing + plain-language UX translation (CAP-5), never touches ports directly. [ADOPTED by user]
- (decision) No side-channel state: tomb-fido2 must never persist its own state (key labels, slot mappings, etc.) in a local sidecar file/db. Anything it needs to remember must be written into the LUKS2 header itself via cryptsetup's token JSON metadata, so break-glass recovery and cross-machine consistency both hold even if the tomb-fido2 binary is gone. [ADOPTED by user]
- (decision) Token JSON schema includes a user-supplied per-key label (e.g. 'YubiKey backup'), written at enroll time, read/displayed at revoke time. [ADOPTED by user]
- (note) Working the deferred secret-hygiene constraint (SPEC: 'best-effort attempt to avoid decrypted key material leaking/persisting in process memory')
- (decision) Secret hygiene resolution: any subprocess invocation that may involve secret entry (existing LUKS passphrase during enroll auth, FIDO2 PIN) runs with inherited/passthrough stdio - tomb-fido2 never captures or pipes that data through its own process memory. Only non-secret output (device lists, credential IDs, labels, exit codes) is captured/parsed. No zeroize/mlock machinery needed since no secret buffer ever exists in-process. [ADOPTED by user] Satisfies SPEC's deferred-to-architect memory-hygiene constraint.
- (decision) Distribution: GitHub Releases with prebuilt binaries, likely automated via release-please for versioning/changelog; local dev via 'make build' wrapping 'cargo build --release'. [ADOPTED by user]
- (decision) Shared pre-flight gate: CAP-6 implemented as one domain::preflight check invoked first by every workflow (unlock/enroll/revoke); no workflow proceeds on failure. [ADOPTED by user, low-stakes default]
- (decision) No persistent logging/telemetry: stderr-only ephemeral human output, no log files. [ADOPTED by user, low-stakes default]
- (decision) Last-keyslot guard (already fixed as a WHAT-level constraint in SPEC) placed architecturally in domain::workflows::revoke: counts valid keyslots via LuksBackend before calling remove_key; aborts before any mutating call if count <= 1. Binds CAP-3.
- (decision) No volume registry: corollary of no-side-channel-state AD. CLI always takes an explicit device/file path argument per invocation; tomb-fido2 keeps no list of 'known tombs' anywhere.
- (version) Verified current versions (web, 2026-07-22): rustc/cargo 1.90.0 (local); clap 4.6.2; serde 1.0.228 + serde_json; thiserror 2.0.18; anyhow 1.0.103; cargo-dist ~0.32.x (axodotdev/cargo-dist, active); release-please (googleapis/release-please, language-agnostic, supports Rust via Cargo.toml manifest).
- (event) spine drafted (v1): 5 ADs, hexagonal paradigm, stack table, structural seed, capability map, 3 deferred items. Proceeding to reconcile + reviewer gate.
- (decision) Testing strategy: domain workflows/guardrails unit-tested against one shared fake LuksBackend/Fido2Backend implementation (test-support module), run in default CI. Separate integration suite exercises real adapters::exec against real cryptsetup/systemd-cryptenroll/fido2-token + real hardware; excluded from default CI, run manually via e.g. 'make test-hardware'. [ADOPTED by user]
- (event) Reviewer gate complete: reconcile + rubric + versions + adversarial all returned. Applying clear fixes: AD-1 attribution fix (systemd owns fido2 token plugin), AD-2 tightened (reuse systemd-fido2 token type, namespaced label field), AD-5 tightened (valid-keyslot definition, live-count rule, token-then-keyslot removal order), AD-3 clarified (capture vs passthrough are separate calls), AD-4 tightened (preflight as first statement in each workflow), new AD-6 (scope fences: no fallback auth, no backup awareness, fixed key-presence timing), new AD-7 (testing strategy), stack table fido2-token row + Rust MSRV relabel.
- (event) spine finalized: 7 ADs (paradigm: hexagonal ports & adapters), stack table, structural seed, capability map, 3 deferred items. status: final
- (event) Renderings & polish: wrote public README.md at repo root (philosophy, capabilities, boundaries, install, security model, break-glass procedure, backup disclaimer, status); ran bmad-editorial-review-structure (1 merge applied: Security model + Break-glass recovery unified) and bmad-editorial-review-prose (2 minor fixes applied) on it. Spine itself untouched by doc polish.
- (decision) Update: added flake.nix (nixpkgs-unstable + flake-utils) providing a devShell with rustc/cargo/clippy/rustfmt/rust-analyzer plus cryptsetup/systemd/libfido2 (for the local hardware-gated integration suite, AD-7) - keeps the Rust toolchain and runtime tools out of the user's system profile. Verified live via 'nix develop': rustc 1.96.1, cryptsetup 2.8.6, libfido2 1.17.0. [ADOPTED by user]
- (event) Update closed: flake.nix devShell added to Stack + Structural Seed, README Installation section updated to mention nix develop.
- (event) Update opened: SPEC.md expanded CAP-1..7 -> CAP-1..11 (added CAP-8 create, CAP-9 close, CAP-10 resize, CAP-11 read-only unlock; amended CAP-1/4/7). Spine was still against the original 7-cap SPEC.
- (decision) New port: FilesystemBackend (mkfs, growfs, mount, umount), alongside existing LuksBackend/Fido2Backend. Binds: CAP-8, CAP-9, CAP-10. Prevents: filesystem ops landing inline in adapters::exec with no fake, silently breaking AD-7's testing strategy; also prevents a second, inconsistent notion of 'port' being invented for filesystem concerns. [ADOPTED by user]
- (decision) FilesystemBackend trait is parameterized by filesystem type (enum), not fixed to one fs. v1 implements only Ext4 in adapters::exec (mkfs.ext4, resize2fs); adding a new variant later is additive (new enum arm + adapter match arm), no port signature change, no domain change. Binds: CAP-8, CAP-10. Prevents: a v1-only-shaped port signature that would require a breaking change to add a second filesystem later. [ADOPTED by user, confirmed ext4-only for v1]
- (decision) Filesystem type is recorded as a new 'filesystem' field on the same per-key token JSON metadata object (AD-2's existing token slot), written once at create time. Binds: CAP-10. Prevents: introducing side-channel state (a registry, a config file) just to remember which growfs tool to invoke at resize time, which would contradict AD-2's no-side-channel-state rule; also prevents re-asking the user or fragile fs-type sniffing via blkid at resize time. [ADOPTED by user]
- (decision) Create workflow bootstrap-keyslot lifecycle (CAP-8): generate a random passphrase in-process -> luksFormat with it (transient keyslot) -> luksOpen using that passphrase to get the mapper -> mkfs (via FilesystemBackend) -> systemd-cryptenroll the real FIDO2 key (real keyslot+token) -> remove the transient passphrase keyslot. Binds: CAP-8. Prevents: a bare cryptsetup luksFormat having no seed keyslot at all (impossible), and prevents the transient passphrase surviving anywhere past this workflow (argv, disk, a leftover keyslot) -- it exists only long enough to seed the header and open the mapper for mkfs, then is destroyed on both ends (keyslot removed, in-memory buffer zeroized). [ADOPTED by user]
- (decision) AD-3 amended: the transient bootstrap passphrase in the create workflow (see prior decision) is a scoped, explicit exception to 'no secret buffer ever exists in-process' -- it is the one path where tomb-fido2 itself generates and briefly holds a secret, because there is no user-supplied passphrase to pass through for a brand-new tomb. Adds a rule: this buffer must use 'zeroize' and be wiped immediately after the luksOpen call that consumes it, before mkfs runs. Adds 'zeroize' crate to Stack. Binds: CAP-8. Prevents: this one necessary exception quietly widening into a general excuse to buffer secrets elsewhere in the codebase.
- (decision) Create's transient-keyslot removal reuses the same live-keyslot-count guard primitive as AD-5's revoke guard (not a separate unprotected removal call), even though at that point in create there are always exactly 2 valid slots (transient + newly-enrolled FIDO2) so the guard trivially passes. Binds: CAP-8. Prevents: two independent, potentially-inconsistent implementations of 'remove a keyslot safely' existing in the codebase -- one guarded (revoke), one not (create's cleanup) -- where a future edit to the guard's logic could be applied to only one.
- (decision) Resize workflow ordering (CAP-10): domain::workflows::resize rejects any shrink request before touching any adapter (grow-only is enforced in domain, not left to cryptsetup/resize2fs's own behavior). On a valid grow request: LuksBackend resizes the LUKS2 mapping first, then FilesystemBackend grows the filesystem on the now-larger mapper. Binds: CAP-10. Prevents: growing the filesystem before the underlying mapper has the extra space (which would corrupt or truncate), and prevents a shrink request reaching cryptsetup/resize2fs at all given SPEC's non-goal (grow-only).
- (decision) Read-only unlock (CAP-11): a single read_only: bool parameter enters domain::workflows::unlock (from the CLI flag) and is passed atomically to both LuksBackend::open (cryptsetup luksOpen --readonly, block-device-level) and FilesystemBackend::mount (mount -o ro, filesystem-level) -- there is no code path where only one of the two is set. Binds: CAP-11. Prevents: a partial implementation (e.g. ro mount over a rw dm-crypt mapping) that would satisfy a filesystem-level-only reading of 'read-only' but not SPEC's explicit block-device-level requirement, and prevents a later remount -o rw succeeding since the underlying mapper itself already refuses writes.
- (decision) CAP-9 (close) needs no new AD: it is FilesystemBackend::umount followed by LuksBackend::close, symmetric to unlock's mount+open pairing, already governed by AD-1 (orchestrator) and AD-4 (mandatory preflight first).
- (direction) AD-1, AD-2, AD-6's 'Binds: all (CAP-1..7)' extend to 'all (CAP-1..11)'. AD-4's preflight binding extends transitively to CAP-8/9/10/11 as well (preflight must still run first in create/close/resize/read-only-unlock workflows).
- (question) Which filesystem types beyond ext4 tomb-fido2 will eventually support is not specified anywhere in SPEC.md. Deferred: v1 ships ext4 only (user-confirmed); FilesystemBackend's enum-based design (see decision above) keeps adding a second fs type additive rather than a breaking change.
- (version) Verified current version (web, 2026-07-22): zeroize 1.8.2 (crates.io, released ~4 months prior).
- (event) Re-distilled spine (v2): added AD-8 (FilesystemBackend port), AD-9 (create bootstrap-keyslot lifecycle), AD-10 (resize ordering/grow-only), AD-11 (read-only atomic propagation); amended AD-1/2/4/6/7 binds to CAP-1..11; amended AD-3 with bounded bootstrap-secret exception; amended AD-5 to note reuse by create's cleanup; extended token JSON schema (tomb_fido2_filesystem field), Stack (zeroize 1.8.2, e2fsprogs), Structural Seed (create/close/resize workflows, filesystem_backend port, hardware tests), Capability map (CAP-8..11), Deferred (filesystem types beyond ext4). AD-1..7 IDs kept stable, none renumbered. Proceeding to reconcile + reviewer gate.
- (event) Reviewer gate run on v2 (reconcile-vs-SPEC, rubric walker, version-verification lens, adversarial divergence-hunt lens). Full outputs in reviews/review-rubric-v2.md; findings triaged below, clear fixes applied at Update-close per skill instructions.
- (decision) New AD-12: deterministic dm-crypt mapping name, derived from the canonicalized input device/file path (stable hash, prefixed tombfido2-), never user-supplied/random and never stored. Binds: CAP-1, CAP-9, CAP-10, CAP-11. Prevents: two units diverging on how unlock's opened mapping gets found again by close/resize/read-only-unlock given AD-2 forbids a lookup registry -- close/resize reconstruct the identical name from the same path argument alone. Mountpoint is not stored either: FilesystemBackend::umount takes the mapper device path and resolves the live mountpoint via the kernel's own mount table (e.g. findmnt against /dev/mapper/<name>), never a remembered path.
- (decision) AD-8 amended: Binds widened to CAP-1, CAP-8, CAP-9, CAP-10, CAP-11 (unlock's mount and read-only unlock's mount both go through FilesystemBackend too, not just create/close/resize). Rule gains a close-ordering clause: domain::workflows::close calls FilesystemBackend::umount (via AD-12's derived name) before LuksBackend::close -- reverse order would hit a busy-device error from a still-mounted filesystem.
- (decision) AD-9 amended for three gaps an adversarial review found: (a) port boundary -- the generate-passphrase/format/open sequence is exposed as ONE port method, LuksBackend::bootstrap_format_and_open(path, filesystem) -> MapperHandle, implemented entirely inside adapters::exec, so the Zeroizing passphrase buffer never crosses into domain (resolves an ambiguity where two compliant implementations could produce incompatible trait shapes); (b) preflight (AD-4) is explicitly the true first call, before this bootstrap method, not merely implied by AD-9's own numbered list; (c) create refuses to run (fails preflight-adjacent check) if the target path already has an existing LUKS2 header, so it never silently overwrites a foreign volume. Binds: CAP-8.
- (question) Deferred: a crash between AD-9's bootstrap_format_and_open and the FIDO2 enroll/cleanup steps leaves an unrecoverable tomb (the transient passphrase is gone, no FIDO2 key enrolled yet). v1 does not implement resume/idempotent-retry for a partial create -- the operator must treat the backing file/partition as one-shot until create completes successfully. Revisit if this proves risky in practice; not required by SPEC.md, which is silent on re-entrant create.
- (decision) AD-10 amended: Rule gains (a) resize's 'current size' is always read live from LuksBackend/FilesystemBackend at decision time, never cached, mirroring AD-5's live-count pattern -- prevents a prior interrupted resize (mapping/filesystem sizes diverged) from being masked; (b) for a file-backed tomb, resize first grows the backing file itself (e.g. truncate/fallocate) before LuksBackend::resize touches the LUKS2 mapping; for a raw block device/partition, tomb-fido2 does not resize the partition table -- it assumes the partition is already large enough and errors clearly if not, since partition management is out of scope (CAP-7 requires operations to work unmodified against raw devices, not that tomb-fido2 manages their sizing).
- (decision) AD-11 amended: Rule gains a rollback clause -- if LuksBackend::open(--readonly) succeeds but the subsequent FilesystemBackend::mount(ro) fails, domain::workflows::unlock calls LuksBackend::close on the just-opened mapping before returning the error, so a failed read-only unlock never leaves a dangling open mapper.
- (question) AD-2's core mechanism (bolting tomb_fido2_label/tomb_fido2_filesystem fields onto the systemd-owned systemd-fido2 LUKS2 token JSON) is not yet verified against cryptsetup's actual token-schema strictness -- web search did not confirm whether cryptsetup/systemd's fido2 token plugin tolerates or rejects unknown JSON fields on its own token type. Resolution: AD-2 amended with an explicit fallback -- if a pre-implementation spike shows the plugin rejects unknown fields, tomb-fido2 instead writes a second, sibling LUKS2 token of a distinct custom type (e.g. tomb-fido2-meta) referencing the same keyslot number, still inside the LUKS2 header, still no sidecar file, preserving AD-2's no-side-channel-state invariant either way. Flagged as an open item to confirm with a throwaway cryptsetup token import/export spike before CAP-2/CAP-8 implementation begins.
- (decision) Deferred item on concurrent invocations cross-referenced to AD-5: a concurrent create/enroll/revoke racing another process's revoke could defeat AD-5's live-count guard (TOCTOU) since cryptsetup takes no invocation-level lock tomb-fido2 relies on beyond the header lock it already takes per-operation. Still deferred (single-user tool, low likelihood) but now explicitly named as the risk rather than a generic 'not addressed.'
- (version) Reviewer-verified current versions (web, 2026-07-22), correcting the prior snapshot: zeroize 1.9.0 (was listed 1.8.2, now fixed); clap 4.6.4; serde/serde_json 1.0.229; thiserror 2.0.19; anyhow 1.0.104; release-please v17.10.4 (replaces the non-falsifiable 'current' label). Rust 1.90.0 confirmed correctly framed as MSRV floor (real current stable 1.97.1, not claimed as current); cargo-dist ~0.32.x, cryptsetup 2.8.6, systemd 261, libfido2 1.17.0 all confirmed still current.
- (event) Capability -> Architecture Map fixes: CAP-1 row gains AD-8 (mount is part of unlock); added explicit constraint row for 'filesystem operations use only standard tools, no custom handling' -> AD-8.
- (event) Update closed. Spine now covers CAP-1..11: 12 ADs total (AD-1..7 stable/amended, AD-8..12 new), 3 ports (LuksBackend, Fido2Backend, FilesystemBackend), 6 domain workflows (create/unlock/enroll/revoke/close/resize). Lint clean, reviewer gate run (reconcile + rubric + version + adversarial lenses), all findings triaged and clear fixes applied in this pass. status: final, updated: 2026-07-22. Known open items carried forward (not blockers): AD-2 token-schema-tolerance needs a pre-implementation spike; AD-9 partial-create crash recovery deferred; concurrent-invocation TOCTOU deferred. README.md (companion deliverable) and _bmad-output/planning-artifacts/epics.md were both written against the original 7-capability spine and are now stale -- not updated in this pass, flagged to user as next steps.
- (event) README.md updated for CAP-8..11: capability table gained Create/Read-only unlock/Close/Resize rows (reordered to group unlock variants together); Security model gained filesystem-tooling and bootstrap-passphrase-exception paragraphs, cross-linked from the 'No fallback authentication' bullet; Installation/break-glass sections gained mount/umount/close/read-only manual command equivalents, with a scope note that create/resize are maintenance ops not covered by break-glass. Ran bmad-editorial-review-structure (3 fixes: table reorder, cross-link, caveat un-buried) and bmad-editorial-review-prose (2 fixes: +/- separator consistency, dehedged a sentence) on it, same as the original v1 doc-polish pass. epics.md deliberately left untouched per user -- will be reworked later with the PM.
- (event) Update opened: SPEC.md further amended CAP-8 (create) with file-backed vs device-backed branching, tool-allocated backing file (tomb-dig-style), device --size (optional, default full capacity, must not exceed), hard refuse on existing file destination / existing LUKS2 header on device, mandatory device wipe-confirmation prompt; plus new placeholder-name constraint (product name 'tomb-fido2' must not be hardcoded into CLI/binary/package name, user-facing strings, or on-disk metadata field names). Spine (v2, CAP-1..11) and README both predate this; reworking now.
- (decision) New AD-13: placeholder-name isolation. Binds: all (CAP-1..11). Prevents: two independently-built units baking the placeholder product name into on-disk field names, the mapping-name prefix, or the CLI/binary/package name, forcing a scattered rename later. Rule: (1) token JSON field names are generic — key_label, filesystem, credential_id, created_at — never prefixed with the product name; (2) AD-12's mapping-name prefix is a separate fixed constant defined once in code, not derived from the product name, so the two can be renamed independently; (3) the CLI binary name and any user-facing name string are read from a single source (Cargo package name), never duplicated as string literals elsewhere; (4) this constraint governs implementation identifiers only — architecture/planning docs may keep referring to the project as tomb-fido2 for continuity. [decision by architect, per SPEC's new placeholder-name constraint]
- (decision) AD-2 amended: token JSON field names renamed off the placeholder per AD-13 — tomb_fido2_label -> key_label, tomb_fido2_filesystem -> filesystem. Same systemd-fido2 token-reuse mechanism and no-side-channel-state rule otherwise unchanged. Binds: CAP-1..11 (unchanged).
- (decision) AD-12 amended: the dm-crypt mapping-name prefix is now specified as a fixed constant string defined once in code, independent of the product's own name (not literally 'tombfido2-' anymore) — satisfies AD-13. Derivation (canonicalized-path hash, never stored/user-supplied) unchanged.
- (decision) AD-9 (create) rewritten for CAP-8's two target modes, both after preflight (AD-4): (a) file-backed — domain checks FilesystemBackend::path_exists(path); refuses before touching anything if true; otherwise calls FilesystemBackend::set_backing_file_size(path, size) to allocate the backing file at the user-given size (no dd/fallocate/truncate by the user, tomb-dig-style). (b) device-backed — domain checks LuksBackend::has_luks2_header(path); refuses before touching anything if true; resolves size = user-given size (must not exceed FilesystemBackend::device_capacity(path), rejected if it does) or that full capacity if omitted; then domain::workflows::create requires an explicit confirmed: bool (mirroring AD-11's read_only bool pattern) — the cli layer always shows the wipe/data-loss warning and gets explicit user confirmation before passing confirmed=true, and domain itself refuses to proceed without it (not merely a cli-side courtesy, so no future cli entry point can skip the prompt). Both modes then proceed into the unchanged bootstrap_format_and_open(path, size, filesystem) -> MapperHandle port method (existing transient-passphrase/mkfs/enroll/cleanup sequence, AD-3/AD-5 exception preserved) — port signature gains the resolved size parameter so cryptsetup constrains the LUKS2 payload to it on device targets. Binds: CAP-8 (unchanged).
- (decision) Two new port methods to support AD-9's mode branching, kept on the existing 3 ports (no 4th port) per Rule of Three: LuksBackend gains has_luks2_header(path) -> bool; FilesystemBackend gains path_exists(path) -> bool, device_capacity(path) -> u64, and set_backing_file_size(path, size) — the last one unifies create's file allocation with AD-10's previously-unspecified 'grows the backing file itself' resize step into one named primitive, used at both call sites. All four are unit-testable via AD-7's existing fake-port strategy, same as every other port method.
- (decision) AD-10 amended: the file-backed grow step now names FilesystemBackend::set_backing_file_size(path, new_size) explicitly (previously only 'grows the backing file itself, e.g. fallocate'), reusing the same primitive AD-9's create now uses for initial allocation. No other change to resize ordering (live-size read, grow-only rejection, LUKS mapping resize before filesystem growfs).
- (event) Re-distilled spine (v3): added AD-13 (placeholder-name isolation); amended AD-2 (field renames key_label/filesystem), AD-9 (file-backed vs device-backed branching, path_exists/has_luks2_header refusal, device size resolution + confirmed:bool gate, bootstrap_format_and_open gains size param), AD-10 (names set_backing_file_size explicitly), AD-12 (mapping prefix decoupled from product name); added 4 new port methods (LuksBackend::has_luks2_header; FilesystemBackend::path_exists/device_capacity/set_backing_file_size) on the existing 3 ports, no 4th port added; Stack gained util-linux/blockdev; token JSON schema diagram field renames; Capability map CAP-8 row + new placeholder-name constraint row. AD-1..12 IDs kept stable, none renumbered. Proceeding to lint + reviewer gate.
- (event) Reviewer gate v3 run (reconcile-vs-SPEC, adversarial, version-verification, rubric walker; full outputs in reviews/review-*-v3.md). Findings triaged: (1) confirmed:bool scope ambiguity in AD-9 -- fixed by splitting CreateTarget into File{path,size}/Device{path,size,confirmed} enum variants so the field only exists on the device branch; (2) AD-12 hash/canonicalization left unpinned -- fixed by mandating one shared domain::mapping_name helper (realpath + hash) called by every workflow, added to Structural Seed; (3) orphan created_at/credential_id schema fields -- fixed by stating their producer/consumer in AD-2; (4) AD-4 implied four workflow functions where read-only unlock is really AD-11's same unlock function -- reworded. Two cosmetic items left unfixed (AD-2 open-item-under-status-final tension; CI runner not named in Stack) -- both low-value, noted in review-rubric-v3.md. Lint clean (0 findings) both before and after these fixes.
- (event) Update closed. Spine v3: 13 ADs total (AD-1..12 stable/amended, AD-13 new: placeholder-name isolation); 4 new port methods (LuksBackend::has_luks2_header; FilesystemBackend::path_exists/device_capacity/set_backing_file_size) on the existing 3 ports; CreateTarget enum resolves the file-backed/device-backed branching at the type level. Lint clean, reviewer gate v3 run and all findings triaged/applied. status: final, updated: 2026-07-22. Next: README.md update for the same SPEC diff (create's two target modes + wipe confirmation; de-hardcode tomb-fido2 from Security-model prose where it names implementation identifiers, keep it as the project's working title everywhere else per AD-13's doc-continuity carve-out).
- (event) README.md updated for the same SPEC diff: working-title disclaimer added under the H1 (placeholder name, don't read into it); Create capability row expanded for the file-backed/device-backed modes and --size semantics; new paragraph on create's refuse-on-existing/refuse-on-LUKS2-header/mandatory-device-wipe-confirmation safety behavior, placed after the existing raw-partition/resize note. Ran bmad-editorial-review-structure (1 condense applied: trimmed the disclaimer's implementation-justification clause, which belongs in AD-13 not the reader-facing blockquote) and bmad-editorial-review-prose (2 fixes: compound-sentence comma, faulty parallelism after 'rather than') on it, consistent with prior doc-polish passes. Spine itself untouched by doc polish.
- (event) Update fully closed: SPEC.md's create-semantics clarification and placeholder-name constraint are now reflected in ARCHITECTURE-SPINE.md (v3, 13 ADs) and README.md. epics.md still not reworked (PM territory, per standing user direction from the prior update).
- (decision) User (via John's review during epics/stories creation) flagged that ARCHITECTURE-SPINE.md lacks the placeholder-name callout SPEC.md and epics.md both carry near their title. Adding an equivalent callout right after the H1 - but worded for this doc's actual convention: unlike SPEC/epics (which reword body prose to 'the tool'), the architecture doc keeps using 'tomb-fido2' throughout its body per AD-13's explicit doc-continuity carve-out (only implementation identifiers must avoid hardcoding it). No AD content changes; cosmetic-only, so Reviewer Gate is skipped per stakes-scaling (throwaway-low-risk single-line addition, no invariant touched).
- (event) Spine re-derived: added placeholder-name callout below H1 title, cross-referencing AD-13's doc-continuity carve-out. No AD/Rule/Deferred content changed; status remains final, updated 2026-07-22.
