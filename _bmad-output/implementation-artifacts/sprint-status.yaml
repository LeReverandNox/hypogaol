# generated: 2026-07-22
# last_updated: 2026-09-10 (dev-story: Story 7.2 started, in-progress)
# project: hypogaol
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-07-22
last_updated: 2026-09-10 (dev-story: Story 7.2 started, in-progress)
project: hypogaol
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  epic-1: in-progress
  1-1-project-scaffolding-nix-devshell: done
  1-2-ci-runs-the-mocked-unit-test-suite: done
  1-3-release-automation: done
  1-4-dependency-preflight-check: done
  1-5-create-a-file-backed-tomb: done
  1-6-create-a-device-backed-tomb: done
  1-7-unlock-and-mount-a-tomb: done
  1-8-unified-cli-dispatch-plain-language-errors: done
  1-9-mount-ux-and-ownership-hardening: done
  epic-1-retrospective: done

  epic-2: in-progress
  2-1-enroll-an-additional-fido2-key: done
  2-2-revoke-a-fido2-key-guarded-against-last-keyslot-lockout: done
  epic-2-retrospective: done

  epic-3: in-progress
  3-1-close-an-unlocked-tomb: done
  3-2-grow-an-existing-tombs-capacity: done
  3-3-unlock-a-tomb-read-only: done
  epic-3-retrospective: done

  epic-4: in-progress
  4-1-view-a-tombs-enrolled-keys-info: done
  4-2-real-progress-reporting-for-create-resize: done
  4-3-enroll-a-fido2-key-with-user-verification: done
  4-4-per-tomb-bind-hooks-exec-hooks-automation: done
  4-5-close-every-open-tomb-close-all: done
  4-6-emergency-slam: done
  epic-4-retrospective: done

  epic-5: in-progress
  5-1-product-identity-rename: done
  5-2-domain-term-rename-tomb-to-volume: done
  5-3-top-level-branding-copy: done
  5-4-update-live-planning-automation-pointers: done
  epic-5-retrospective: done

  epic-6: done
  6-1-crash-safe-create-resume: done
  6-2-custom-key-label-at-create: done
  6-3-hook-template-scaffolding-at-create: done
  6-4-xfs-and-btrfs-filesystem-support: done
  6-5-concurrent-invocation-guard: done
  6-6-proactive-fido2-pin-status-guidance: done
  6-7-one-letter-cli-flag-shorthand: done
  6-8-repository-hygiene-badges: done
  epic-6-retrospective: done

  epic-7: in-progress
  7-1-presence-only-enrollment-up-only-mode: done
  7-2-mount-point-creation-resilient-to-pre-existing-ownership: in-progress
  7-3-touchless-enrollment-flag-precedence-no-up-mode: backlog
  7-4-uv-capability-detection: backlog
  7-5-interactive-unlocking-mode-menu: backlog
  epic-7-retrospective: optional

action_items:
  - epic: 7
    action: "New Story 7.2 (mount-point creation resilient to pre-existing /run/media ownership) inserted between 7.1 and 7.2 (renumbered to 7.3) — udisks2/gvfs-managed machines hit EACCES creating the per-volume mount point under a root-owned /run/media/<user>, surfacing as the generic 'couldn't mount its filesystem' message. Root-caused live via manual mkdir/stat reproduction; fix shape (unconditionally privileged mkdir+chown on the leaf, no reliance on base.exists()) taken from dyne/tomb's proven fix for the identical conflict (their issue #461)."
    owner: "Amelia"
    status: open
  - epic: 7
    action: "Process gap found while root-causing the Story 7.2 mount bug: Story 1.9's own review record (1-9-mount-ux-and-ownership-hardening.md:54) marked a '[Review][Patch]' finding [x] and claimed it fixed ('gate the mkdir+chown on a uid stat comparison instead of base.exists()') — but git blame on the current code shows that fix was never actually implemented; the block is untouched since the original 9065e2c commit, and the cited follow-up commit (c005771) never touched src/adapters/exec/mod.rs. Same family as the epic-5/epic-6 self-reported-count watch items already on file (a story's own claim didn't match a from-scratch re-check), but this instance is worse: it's a claimed *code fix*, not just a count, and it silently regressed real user-facing behavior for over six weeks with no test catching it. Worth a retro discussion: should code-review 'fixed' patch findings require a diff citation (file:line) in the story doc, not just a checkbox, so this class of gap is mechanically checkable later?"
    owner: "unassigned"
    status: open
  - epic: 7
    action: "Story 7.1's PR (#76) is the first since Codecov was activated (Story 6.8) to actually surface patch-coverage findings — codecov.yml now excludes src/adapters/exec/mod.rs and src/cli/main.rs wholesale (their dispatch/subprocess-calling functions are exercised only by make test-hardware, never by CI's make coverage). Known tradeoff, not yet resolved: both files also hold genuinely unit-tested pure functions (fido2_verification_args, parse_client_pin_configured, parse_size, confirms_wipe, device_create_confirmation, etc.) — excluding the whole file means a future regression in one of those, shipped without a matching test, no longer shows up as a coverage drop either. LeReverandNox wants to re-challenge this with Winston — a more surgical fix (splitting hardware-only code into its own submodule so only that gets ignored) is possible but needs proper scoping, not a bolt-on."
    owner: "Winston"
    status: open
  - epic: 7
    action: "Story 7.1's own stated baseline (Dev Notes/Task 9: 'cargo clippy --all-targets → 7 warnings, all pre-existing too_many_arguments') was wrong when independently re-verified: `git stash` + `cargo clippy --all-targets` against the story's own `baseline_commit` (`41be34f`) shows 5, not 7. Same recurring pattern as the epic-5/epic-6 self-reported-count watch items (below) — a story's own doc claimed a number that a from-scratch re-check contradicted. No functional impact (this story's post-implementation count of 6 is consistent with the corrected baseline of 5 plus one expected new warning on `enroll::run`), but the automation action item below (epic 6, 'Automate the test-count/clippy-warning line...') remains unaddressed and this is a fresh data point for it."
    owner: "Amelia"
    status: open
  - epic: 7
    action: "Story 7.1's Task 4 real-hardware spike (confirm whether an explicit --client-pin=false enrollment ceremony itself still prompts for a PIN against a PIN-configured device, mirroring the already-hardware-confirmed user_verification=true case) was not completed in the dev-agent session — two independent environment gaps: sudo requires an interactive password this non-interactive dev-agent session can't supply, and this Nix dev shell's cryptsetup also lacks the systemd-fido2 external token plugin (confirmed via a real, harmless CLI invocation that correctly failed preflight on this). RESOLVED 2026-09-10 (LeReverandNox): ran `hypogaol create file --size=64M --client-pin=false tomb-pin-spike` live, outside the Nix devshell, against a PIN-configured TOKEN2 key (/dev/hidraw3). No PIN prompt appeared at any point — only the two standard presence-confirmation hints — and the resulting volume unlocked fine afterward with touch alone, matching the already-confirmed user_verification=true case exactly. print_enroll_pin_warning's client_pin==Some(false) wording updated from hedged to a flat statement accordingly."
    owner: "LeReverandNox"
    status: done
  - epic: 6
    action: "Automate the test-count/clippy-warning line Completion Notes cite (e.g. a make target that prints 'N total (X lib + Y unit)' and the current too_many_arguments warning count directly from cargo test/cargo clippy output) so a story copy-pastes a real number instead of hand arithmetic — Epic 6 retro (2026-08-11) found 6 stories in a row (6.2 through 6.7) where a self-reported baseline/count was wrong, each caught only by a subsequent story's from-scratch re-verification. The existing Epic 5 watch item (below) has kept this from causing real harm, but it's pure review overhead; this item aims at the root cause instead."
    owner: "Amelia"
    status: open
  - epic: 6
    action: "Sync sprint-status.yaml's action_items entries in the same pass a story's Completion Notes resolves one — Epic 6 retro (2026-08-11) found 3 stale entries (Story 6.1's crash-simulation gap, and the Epic 3/4 release-pipeline verifications) that were actually completed and documented elsewhere (story Completion Notes, a real GitHub release) but never marked done here."
    owner: "Amelia"
    status: open
  - epic: 6
    action: "Carry the AD-20 lesson forward: any future feature adding locking/synchronization around an adapters::exec subprocess call must be checked against cryptsetup's and systemd-cryptenroll's own internal locking behavior at design time, not discovered live — Story 6.5's flock(2) design self-deadlocked against both tools' internal locking and was replaced with a Linux abstract-namespace AF_UNIX socket mid-story. Already documented in ARCHITECTURE-SPINE.md's AD-20; this item is a reminder to read it next time, not new work."
    owner: "Winston"
    status: open
  - epic: 6
    action: "Manually verify Story 6.5's concurrent-invocation guard (AC #1) against real hardware with two genuine OS processes — this dev/review session could not: no assert_cmd-style plumbing exists in tests/hardware/main.rs to spawn CARGO_BIN_EXE_hypogaol as a child process, and no interactive sudo/physical FIDO2 touch was available. tests/unit/lock_target.rs already proves the underlying flock-free abstract-socket lock mechanism contends/releases correctly within one process (two independently-opened file descriptors), but the literal two-real-process revoke scenario is unverified. Run two concurrent `revoke` invocations (or the in-process two-thread fallback Story 6.5's Task 13 describes) against a real volume with 2 valid FIDO2 keyslots and confirm exactly one succeeds while the other reports the lock-contention message. RESOLVED 2026-08-10 (LeReverandNox): re-ran `make test-hardware` and a concurrent two-process revoke script against a real volume with 2 valid FIDO2 keyslots — exactly one succeeded, the other reported the lock-contention message. Also manually verified the `close_all`/`slam`+`lock_mapping` fix (2026-08-10 review finding) directly: deleted a mapping's backing file while it was still open, then ran `close-all`/`slam` — both closed it successfully instead of hard-failing to lock."
    owner: "LeReverandNox"
    status: done
  - epic: 6
    action: "Manually crash-simulate Story 6.1's marker-verified resume against real hardware — a physical FIDO2 token was detected in the dev-story execution environment (fido2-token -L), but the crash-simulation scenario needs interactive touch/PIN confirmation mid-create, which the non-interactive dev-agent session could not provide. Verify both file-backed and device-backed: interrupt create between luksFormat and final cleanup (kill -9 or a temporary early-return), re-run create against the same destination, confirm it resumes to a fully working, unlockable volume. RESOLVED 2026-08-08 (LeReverandNox), documented in 6-1-crash-safe-create-resume.md's Completion Notes but not previously synced here: a temporary panic! was inserted in bootstrap_and_provision right after bootstrap_format_and_open returns, then reverted. File-backed: confirmed working end-to-end, resumed with no confirmation prompt, produced a fully working, unlockable volume; a subsequent create against the completed volume correctly refused with DestinationExists. Device-backed: confirmed working under sudo (the unprivileged-shellout gap is a separate, already-tracked issue). A real gap was found and fixed in the same session: a crashed attempt's dm-crypt mapping survives process death (kernel object, independent of userspace), which made a resume's luksFormat fail with a busy-device error before marker-verified resume logic ever ran — fixed via a new close_stale_mapping call, unconditional at the top of bootstrap_and_provision, tolerant of the common no-stale-mapping case."
    owner: "LeReverandNox"
    status: done
  - epic: 6
    action: "Activate LeReverandNox/hypogaol on codecov.io (OAuth login + repo enable) so Story 6.8's coverage badge/upload reports real data — an interactive, account-level step no dev agent can complete non-interactively. The CI job and badge markup ship regardless per the story; only the activation itself is deferred here. RESOLVED 2026-08-11 (LeReverandNox): registered on codecov.io, repo activated, token generated and stored as the `CODECOV_TOKEN` repository secret."
    owner: "LeReverandNox"
    status: done
  - epic: 5
    action: "Run `make test-hardware` manually against real hardware to verify Story 5.2's rename (AC #2) — no physical FIDO2 device was available in the dev-story execution environment, so only cargo build/make test/cargo build --test hardware (typecheck) were verified automatically. RESOLVED 2026-08-02 (LeReverandNox): ran against real hardware, 21/22 passed. The 1 failure (resize_rejects_a_shrink_request_and_leaves_the_volume_untouched) is the pre-existing epic-3 grow-only-check bug recurring, not a rename regression — see that action item, reconfirmed a third time by this run."
    owner: "LeReverandNox"
    status: done
  - epic: 5
    action: "Fix the tier-2 resize grow-only check bug (src/domain/workflows/resize.rs's grow-only size comparison rejecting a same-size/near-minimum-size request) — third occurrence (Epic 4's 4.3 hardware run, Epic 5's 5.2 hardware run). LeReverandNox explicitly requested this be addressed before Epic 6 starts. RESOLVED 2026-08-03 (Amelia/LeReverandNox, PR #57): root cause was not mkfs overhead on small volumes as originally suspected — confirmed empirically via cryptsetup luksFormat/luksDump that the LUKS2 header consumes a fixed ~16 MiB regardless of volume size, so filesystem_size (post-header payload bytes) was being compared directly against new_size (whole-file bytes), always understating available payload by the header. Fixed by deriving the header size from values already on hand and converting new_size into payload units before comparing. make test-hardware's 4 resize_* scenarios reconfirmed passing on real hardware by LeReverandNox, including the previously-failing grow-only rejection case."
    owner: "LeReverandNox/Amelia"
    status: done
  - epic: 5
    action: "(Low priority, optional) Clean up the 30 residual tomb-fido2 literals still in tests/unit/*.rs (temp-dir fixture names, cli.rs's fake argv[0] slices) — deliberately deferred by Stories 5.1/5.2 as out-of-scope test fixtures, confirmed harmless."
    owner: "unassigned"
    status: open
  - epic: 5
    action: "Commission Hypogaol mascot/mark artwork from an external image-generation model (BMAD has no image-generation agent/tool available) — brand-identity.md specifies the full visual brief. Always out of Epic 5's scope (explicit non-goal), not a gap."
    owner: "LeReverandNox"
    status: open
  - epic: 5
    action: "Watch for self-reported completion-note claims (grep counts, verified statements) not matching actual grep/build/format-check output as an explicit recurring-pattern check — hit in all 4 Epic 5 stories (5.1's false no-literals claim, 5.2's undetected cargo fmt failures, 5.3's miscounted grep matches), each caught only in review."
    owner: "Amelia"
    status: open
  - epic: 1
    action: "Fix mount-point ownership: chown to invoking uid/gid before chmod 0700 in FilesystemBackend::mount (currently root-owned, root-only)"
    owner: "Amelia"
    status: done
  - epic: 1
    action: "Move mount location from /tmp to /run/media/<username>/<tomb-name>, basename-derived with collision-suffix fallback"
    owner: "Amelia"
    status: done
  - epic: 1
    action: "Change --path from a flag to a positional argument on create file, create device, and unlock"
    owner: "Amelia"
    status: done
  - epic: 1
    action: "Sync a story's Status header field to done immediately after its PR merges"
    owner: "Amelia"
    status: done
  - epic: 1
    action: "Document the sudo requirement for device-backed operations in --help/README before Epic 2's revoke/enroll touch real devices"
    owner: "unassigned"
    status: done
  - epic: 1
    action: "Add a LICENSE file and a branch-protection rule requiring CI to pass before merge. RESOLVED 2026-07-28 (Epic 4 retro): LICENSE done (GPL-3.0-or-later, commit 38ac76a); branch-protection dropped as not applicable — LeReverandNox is not on a GitHub Team/Enterprise plan and cannot configure branch-protection rules on a personal account"
    owner: "LeReverandNox"
    status: done
  - epic: 1
    action: "Resolve the open design question on real step-by-step progress reporting during create/unlock"
    owner: "unassigned"
    status: open
  - epic: 2
    action: "RESOLVED (Winston, 2026-07-25) — full design recorded in 2-1-enroll-an-additional-fido2-key.md's Dev Notes ('Architect consultation resolved'). Summary: replace enroll_fido2_key's temporal-diff device identification (the race behind the hang/'More than one FIDO device found' failures) with a spatial choice — Fido2Backend::enroll_fido2_key gains a Fido2DeviceSelection{Interactive, Explicit{new, existing: Option<PathBuf>}} param; Interactive waits for enough enumerated devices then prompts explicitly for each needed role (scales to N>2, no elimination shortcut); Explicit adds --fido2-device/--unlock-fido2-device flags to enroll (all-or-nothing, for unattended/scripted use). unlock (LuksBackend::open) separately gets a presence-wait loop only (>=1 device, no picker, no new flag, no signature change — cryptsetup's own credential-based auto already disambiguates fine there; user declined an explicit unlock flag for now). create's bootstrap-enroll call (finish_provisioning, same shared enroll_fido2_key fn) is now explicitly in scope too (user-approved 2026-07-25, deliberately overriding the story's original 'no port/signature changes, don't touch create.rs' constraint): gets the same Interactive single-role treatment (silent auto-pick when exactly one device present, so today's single-key UX is unchanged) plus a new scripted --fido2-device flag on create file/create device (no --unlock-fido2-device there, no existing-key role to fill). No architecture-spine amendment needed. DONE — implemented (commits 999b7ef..25d0fb4) and hardware-verified 2026-07-25, all 6 make test-hardware scenarios passing. Two follow-on test-only fixes were needed along the way (neither was a product bug): the enroll regression test was grepping the wrong luksDump form for its custom token fields, and it needed manual key-swap pauses to actually exercise both keys independently rather than letting cryptsetup's auto-matching silently reuse the same one twice. Full detail in the story file's Dev Notes."
    owner: "Amelia"
    status: done
  - epic: 2
    action: "When scoping Story 3.1 (close), explicitly decide the mount-directory rmdir behavior after unmount, rather than letting it fall through unaddressed"
    owner: "Amelia"
    status: done
  - epic: 2
    action: "When a story adds a new workflow reusing shared adapters::exec error strings, explicitly check for marker-bleed into other workflows' plain-language translations before merge (3 real bugs from this pattern across 2 stories so far). GRADUATED to done (Epic 6 retro, 2026-08-11, LeReverandNox): applied proactively in both Story 6.1 (create-marker-write/remove bleed, fixed pre-review) and Story 6.4 (transient-mount bleed, fixed pre-review via Task 10) — zero fresh marker-bleed bugs found in review across the whole epic. Treated as internalized practice, not a standing watch item."
    owner: "Amelia"
    status: done
  - epic: 3
    action: "Document the sudo requirement for device-backed operations in --help/README. DONE 2026-07-28 (Epic 4 retro), commit 38ac76a"
    owner: "Amelia"
    status: done
  - epic: 3
    action: "Add a LICENSE file and a branch-protection rule requiring CI to pass before merge. RESOLVED 2026-07-28 (Epic 4 retro): LICENSE done (GPL-3.0-or-later, commit 38ac76a); branch-protection dropped as not applicable — LeReverandNox is not on a GitHub Team/Enterprise plan"
    owner: "LeReverandNox"
    status: done
  - epic: 3
    action: "Resolve the progress-reporting design question - decide whether to fold into Epic 4 scoping or resolve standalone before Epic 4 starts. RESOLVED — Story 4.2 was itself this action item"
    owner: "LeReverandNox"
    status: done
  - epic: 3
    action: "Decide once, deliberately, whether the repeatedly-deferred swallowed-luks.close-on-rollback pattern (create.rs, resize.rs, unlock.rs) needs fixing or is fine to leave, instead of a fourth workflow deferring it again unexamined. RESOLVED 2026-07-28 (Epic 4 retro): fixed via a new DomainError::RollbackCleanupAlsoFailed variant that wraps the original error and appends the close failure detail, rather than swallowing it or flattening into the marker-sensitive AdapterFailure bucket. Commit 38ac76a"
    owner: "Amelia"
    status: done
  - epic: 3
    action: "Exercise release automation end-to-end against a real tag at least once before treating it as production-ready. RESOLVED (never synced here — found stale during the Epic 6 retro, 2026-08-11): release-please cut hypogaol-v0.2.0 (published 2026-08-03), cargo-dist attached full artifacts (aarch64/x86_64 tarballs + sha256 sums, source archive, dist-manifest.json) — verified live via `gh release view hypogaol-v0.2.0`. Pipeline proven end-to-end."
    owner: "LeReverandNox"
    status: done
  - epic: 3
    action: "BUG (found during Story 4.3's hardware run, 2026-07-27): resize::run's tier-2 grow-only check (resize.rs's grow_open_mapping, fs.filesystem_size comparison) fails to reject a same-size resize request on a small (32 MiB) file-backed tomb — tests/hardware/main.rs's resize_rejects_a_shrink_request_and_leaves_the_tomb_untouched failed with 'expected DomainError::ResizeMustGrow, got Ok(())'. Root cause unconfirmed but suspected: mkfs.ext4 overhead on a near-minimum-size tomb makes the filesystem's own reported size close enough to the full backing-file size that new_size <= live_current_size no longer holds. Introduced in Story 3.2 (PR #29) — unrelated to Story 4.3's changes (neither resize.rs nor this test's assertions were touched by 4.3). Other 17/18 hardware tests passed. RECONFIRMED 2026-08-02 during Story 5.2's hardware run (test renamed to resize_rejects_a_shrink_request_and_leaves_the_volume_untouched, same panic: 'expected DomainError::ResizeMustGrow, got Ok(())', 21/22 other tests passed) — third occurrence (4.3, and now 5.2), still unrelated to whatever story is running at the time; this is a standing tier-2 grow-only-check defect that needs its own fix, not a story-specific regression."
    owner: "unassigned"
    status: open
  - epic: 4
    action: "Watch for 'new pure parsing/logic function ships without a unit test' as an explicit recurring-pattern check, the same way marker-bleed is checked — it hit three times in Epic 4 (4.3's fido2_verification_args, 4.5's loop:/device: field parser, 4.6's fuser -m PID parser), each caught only in review. GRADUATED to done (Epic 6 retro, 2026-08-11, LeReverandNox): applied proactively across Story 6.1 (marker-id extraction), 6.3 (bind-hooks template zero-entries proof), 6.5 (lock_target_path), and 6.6 (PIN-status parser, stderr-signal classifier) — every new parsing function got a direct unit test without review having to ask. Treated as internalized practice, not a standing watch item."
    owner: "Amelia"
    status: done
  - epic: 4
    action: "Push commit 38ac76a to main, merge the resulting release-please PR, and confirm v0.1.0 publishes with artifacts attached via cargo-dist — first real exercise of the release pipeline. RESOLVED (never synced here — found stale during the Epic 6 retro, 2026-08-11): superseded by hypogaol-v0.2.0 (published 2026-08-03, post-rebrand), same pipeline, full cargo-dist artifacts attached — see the epic-3 action item above for the verification."
    owner: "LeReverandNox"
    status: done
